# Quiniela Mundial 2026

App HTML single-file con Firebase Firestore para una quiniela del Mundial 2026 entre amigos.
Dos despliegues comparten el mismo proyecto Firebase y difieren solo en su sala (`ROOM_DOC`)
y su identidad visual (`APP`).

Rama base inicial del repositorio.
